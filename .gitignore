__pycache__/
*.pyc
*.pyo
.env
.venv/
venv/
outstanding-todos.md

# Workspace-specific config (use app.yaml.template)
# app.yaml

# Git worktrees
.worktrees/

# Human tokens (brainstorming notes)
.humantokens/

# Uploaded files (clipboard paste images)
uploads/

# uv lockfile — not portable across PyPI proxies, generate locally with `uv lock`
uv.lock

# Codex CLI generated/cached files (only the bundled model catalog is tracked)
.codex/*
!.codex/databricks-models.json
